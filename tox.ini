[tox]
isolated_build = true
skipsdist=true
envlist = py37,flake8

[testenv]
whitelist_externals = poetry
commands =
    poetry install -v
    poetry run pytest --cov-report term --cov=portfolio_rebalancer tests/

[testenv:flake8]
deps = flake8
commands =
    flake8
